apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Live cluster. Argo CD syncs this path from main.
namespace: our-home

resources:
  - ../../base
  - namespace.yaml
  - certificate.yaml
  - middleware.yaml
  - ingress.yaml
