apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Live cluster. Argo CD syncs this path from main.
namespace: linkding

resources:
  - ../../base
  - namespace.yaml
  - certificate.yaml
  - ingress.yaml
