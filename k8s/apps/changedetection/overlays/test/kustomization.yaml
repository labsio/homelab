apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Validation target: `kubectl apply -k .` on a throwaway k3d cluster, or a
# changedetection-test namespace on the real cluster. Same base as prod; only the
# namespace and hostname differ.
namespace: changedetection-test

resources:
  - ../../base
  - namespace.yaml
  - certificate.yaml
  - ingress.yaml
